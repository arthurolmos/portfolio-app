import { DefaultLayout } from "../../src/components/layout/DefaultLayout";
import styled from "styled-components";

interface Props {}

export default function Rolezator(props: Props) {
  const {} = props;

  return (
    <DefaultLayout backgroundColor="white" color="black">
      ROLEZATOR IS HERE!
    </DefaultLayout>
  );
}
